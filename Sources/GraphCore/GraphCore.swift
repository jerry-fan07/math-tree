/// Version of the compiled-content format produced by `ContentBuild` and
/// consumed by the app. Bumped when `graph.json`'s shape changes.
public enum GraphCore {
    public static let contentFormatVersion = 1
}
